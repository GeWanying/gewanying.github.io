source "https://rubygems.org"

# Matches GitHub Pages' build environment so local preview == deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick" # required for `jekyll serve` on Ruby 3+
gem "ffi", "< 1.17" # keep compatibility with the macOS system Ruby (2.6)
